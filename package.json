{
  "name": "weather-dashboard",
  "version": "1.0.0",
  "description": "Weather Dashboard - Full Stack Application",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start:prod": "cd backend && npm run start:prod"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

